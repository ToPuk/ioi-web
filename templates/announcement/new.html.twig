{% extends 'base.html.twig' %}

{% block title %}Мэдээ мэдээлэл оруулах{% endblock %}

{% block body %}
    <div class="page-header">
        <div class="page-header-content container header-elements-md-inline">
            <div class="page-title d-flex">
                <h4><i class="icon-newspaper mr-2"></i> <span class="font-weight-semibold">Мэдээ мэдээлэл оруулах</span></h4>
                <a href="#" class="header-elements-toggle text-default d-md-none"><i class="icon-more"></i></a>
            </div>

            <div class="header-elements d-none bg-transparent border-0 py-0 mb-3 mb-md-0">
                <div class="breadcrumb">
                    <a href="{{ path('default_index') }}" class="breadcrumb-item"><i class="icon-home2 mr-2"></i> Нүүр</a>
                    <span class="breadcrumb-item active">Мэдээ оруулах</span>
                </div>
            </div>
        </div>
    </div>

<div class="page-content container pt-0">
    <div class="content-wrapper">

        <div class="card">
            <div class="card-body">
                {{ include('announcement/_form.html.twig') }}
            </div>
        </div>


        <a href="{{ path('announcement_index') }}">Мэдээний жагсаалт харах</a>
    </div>
</div>

{% endblock %}
